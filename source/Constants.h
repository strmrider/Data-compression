#ifndef CONSTANTS_H
#define CONSTANTS_H
#include <vector>
#include <queue>
#include <math.h>
#include <cstdlib>
#include <fstream>
#include <iostream>
using namespace std;

enum PRINT_TYPE{PREORDER, LEVEL};
enum HEAP_ORDER { MAX, MIN };
enum VACANCY { NONE, BOTH, RIGHT, LEFT };

#endif // !CONSTANTS_H
